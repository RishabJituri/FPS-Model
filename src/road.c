/**
 * Each lane will be a single layer list. 
 * Size will remain immutable
 * vehicles will traverse in the ascending direction of the list
 * 
 */

typedef struct tile{
    
}

typedef struct lane{
    
};