from ursina import *
from games.zombie_game import *
import threading
g = game()

# game_thread = threading.Thread(target=g.play())
# game_thread.daemon = True  # Make the thread a daemon so it will close with the program
# game_thread.start()
print("success")